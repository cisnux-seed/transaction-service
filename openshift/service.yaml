apiVersion: v1
kind: Service
metadata:
  name: transaction-service
  namespace: one-gate-payment
  labels:
    app: transaction-service
spec:
  selector:
    app: transaction-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
  type: ClusterIP